import XCTest
@testable import App

final class AppTests: XCTestCase {
    func testExample() throws {
        // Minimal placeholder test so the target isn't empty
        XCTAssertTrue(true)
    }
}
